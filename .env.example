# Cloudflare Pages Variables and Secrets (safe placeholders only)
# NOTE: These are placeholders only. Do not put real secrets in git.

# Stripe secrets (Cloudflare Pages > Variables and Secrets; encrypted)
STRIPE_API_KEY=sk_test_replace_in_cloudflare
STRIPE_WEBHOOK_SECRET=whsec_replace_in_cloudflare

# Stripe Price IDs (Cloudflare Pages > Variables and Secrets; encrypted)
STRIPE_PRICE_CORE_MONTHLY=price_replace_in_cloudflare
STRIPE_PRICE_CONTROL_MONTHLY=price_replace_in_cloudflare
STRIPE_PRICE_COMMAND_MONTHLY=price_replace_in_cloudflare

# Optional annual Price IDs
STRIPE_PRICE_CORE_ANNUAL=price_replace_in_cloudflare
STRIPE_PRICE_CONTROL_ANNUAL=price_replace_in_cloudflare
STRIPE_PRICE_COMMAND_ANNUAL=price_replace_in_cloudflare

# Base44 server-to-server resend invite endpoint
BASE44_BACKEND_URL=https://your-base44-backend.example.com/functions
BASE44_API_KEY=replace_in_cloudflare
